package edu.uniquindio.grownupsvet.grownupsvet_backend.user.model;

/** Roles available to an account in the initial application model. */
public enum UserRole {
    OWNER,
    VETERINARIAN,
    ADMINISTRATOR
}
